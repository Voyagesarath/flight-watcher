# fast-flights scrapes Google Flights. Its transitive dep `primp` imports
# typing_extensions but doesn't declare it, so we pin it explicitly —
# otherwise a clean CI install crashes with ModuleNotFoundError.
fast-flights==2.2
PyYAML>=6.0
typing_extensions>=4.0
